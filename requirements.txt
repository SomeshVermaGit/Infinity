# Core dependencies
torch>=2.0.0
deepspeed>=0.10.0
sentencepiece>=0.1.99

# Training utilities
tqdm>=4.65.0
numpy>=1.24.0

# Optional: Flash Attention (requires CUDA and compilation)
# flash-attn>=2.0.0  # Uncomment if you want to use flash attention

# Development
pytest>=7.3.0
black>=23.3.0
